{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run _cli.build.ts",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/src/next/test/_cli.build.ts",
            "args": [
                "${file}"
            ],
            "runtimeExecutable": "ts-node",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}