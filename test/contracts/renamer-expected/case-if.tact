fun function_def_0(x: Int, y: Int): Int {
    if (x < y) {
        return x;
    } else {
        return y;
    }
}

contract contract_1 {
    x: Int as int32;

    init() {
        if (1 > 0) {
            if (2 > 3) {
                self.x = 1;
            } else {
                self.x = 2;
            }
        } else {
            self.x = 0;
        }
    }
}
