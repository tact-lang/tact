#include "constants_ConstantTester.stdlib.fc";
#include "constants_ConstantTester.constants.fc";

tuple $ConstantTester$_contract_init() impure inline_ref {
    tuple $self = null();
    return $self;
}

tuple $ConstantTester$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    __tact_context_sys = $sc~load_ref();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return null();
    } else {
        ;; Allow only workchain deployments
        throw_unless(137, my_address().preload_uint(11) == 1024);
        return $ConstantTester$_contract_init();
    }
}

() $ConstantTester$_contract_store(tuple v) impure inline {
    builder b = begin_cell();
    b = b.store_ref(__tact_context_sys);
    b = b.store_int(true, 1);
    set_data(b.end_cell());
}

int $ConstantTester$_fun_something1(tuple $self) impure inline_ref {
    var ($self) = $self;
    return 10;
}

int $ConstantTester$_fun_something2(tuple $self) impure inline_ref {
    var ($self) = $self;
    return null();
}

int $ConstantTester$_fun_something3(tuple $self) impure inline_ref {
    var ($self) = $self;
    return 123000000000;
}

int $ConstantTester$_fun_something4(tuple $self) impure inline_ref {
    var ($self) = $self;
    return 456000000000;
}

slice $ConstantTester$_fun_something5(tuple $self) impure inline_ref {
    var ($self) = $self;
    return __gen_str_3226689099();
}

int $ConstantTester$_fun_globalConst(tuple $self) impure inline_ref {
    var ($self) = $self;
    return 100;
}

;;
;; Receivers of a Contract ConstantTester
;;

;;
;; Get methods of a Contract ConstantTester
;;

_ %something1() method_id(121234) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_something1(self);
    return res;
}

_ %something2() method_id(125425) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_something2(self);
    return res;
}

_ %something3() method_id(129488) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_something3(self);
    return res;
}

_ %something4() method_id(100663) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_something4(self);
    return res;
}

_ %something5() method_id(104726) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_something5(self);
    return res;
}

_ %globalConst() method_id(116577) {
    var self = $ConstantTester$_contract_load();
    var res = $ConstantTester$_fun_globalConst(self);
    return res;
}

_ supported_interfaces() method_id {
    return (
        "org.ton.introspection.v0"H >> 128,
        "org.ton.abi.ipfs.v0"H >> 128,
        "org.ton.deploy.lazy.v0"H >> 128,
        "org.ton.debug.v0"H >> 128,
        "org.ton.chain.workchain.v0"H >> 128
    );
}

_ get_abi_ipfs() {
    return "ipfs://QmdftDuW7kgeUUjCagbwaBAY9Jijbpaj5L4qGqMRS1CDvR";
}

_ lazy_deployment_completed() {
    return get_data().begin_parse().load_int(1);
}

;;
;; Routing of a Contract ConstantTester
;;

(tuple, int) $ConstantTester$_contract_router(tuple self, int msg_bounced, slice in_msg) impure inline_ref {
    
    ;; Parse incoming message
    int op = 0;
    if (slice_bits(in_msg) >= 32) {
        op = in_msg.preload_uint(32);
    }
    
    ;; Handle bounced messages
    if (msg_bounced) {
        return (self, true);
    }
    
    return (self, false);
}

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    var msg_flags = cs~load_uint(4);
    var msg_bounced = ((msg_flags & 1) == 1 ? true : false);
    slice msg_sender_addr = __tact_verify_address(cs~load_msg_addr());
    __tact_context = (msg_bounced, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var self = $ConstantTester$_contract_load();
    
    ;; Handle operation
    int handled = self~$ConstantTester$_contract_router(msg_bounced, in_msg);
    
    ;; Throw if not handled
    throw_unless(handled, 130);
    
    ;; Persist state
    $ConstantTester$_contract_store(self);
}
