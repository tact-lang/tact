global (int, slice, int, slice) __tact_context;
global cell __tact_context_sys;

builder __gen_write_C(builder build_0, (int) v) inline_ref {
    var (v'a) = v;
    build_0 = build_0.store_int(v'a, 257);
    return build_0;
}

(slice, ((int))) __gen_read_C(slice sc_0) inline_ref {
    var v'a = sc_0~load_int(257);
    return (sc_0, (v'a));
}

(int) __gen_load_C() inline_ref {
    slice sc = get_data().begin_parse();
    __tact_context_sys = sc~load_ref();
    return sc~__gen_read_C();
}

() __gen_store_C((int) v) impure inline_ref {
    builder b = begin_cell();
    b = b.store_ref(__tact_context_sys);
    b = __gen_write_C(b, v);
    set_data(b.end_cell());
}

((int), ()) $__gen_C_receive_comment_b41a9933301c1d728e64e23685fe3a9c7540ab9359ef7318943c0ee5e0910d5d((int) $self) impure {
    var ($self'a) = $self;
    return (($self'a), ());
}


() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Parse incoming message
    int op = 0;
    if (slice_bits(in_msg) >= 32) {
        op = in_msg.preload_uint(32);
    }
    var cs = in_msg_cell.begin_parse();
    var msg_flags = cs~load_uint(4);
    var msg_bounced = ((msg_flags & 1) == 1 ? true : false);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounced, msg_sender_addr, msg_value, cs);
    
    ;; Handle bounced messages
    if (msg_bounced) {
        return ();
    }
    
    ;; Text Receivers
    if (op == 0) {
        var text_op = slice_hash(in_msg);
        
        ;; Receive "Message" message
        if (text_op == 0xb41a9933301c1d728e64e23685fe3a9c7540ab9359ef7318943c0ee5e0910d5d) {
            var self = __gen_load_C();
            self~$__gen_C_receive_comment_b41a9933301c1d728e64e23685fe3a9c7540ab9359ef7318943c0ee5e0910d5d();
            __gen_store_C(self);
            return ();
        }
    }
    
    throw(130);
}

_ supported_interfaces() method_id {
    return (
        "org.ton.introspection.v0"H >> 128,
        "org.ton.abi.ipfs.v0"H >> 128
    );
}

_ get_abi_ipfs() {
    return "ipfs://QmUqd2wkm7B9aeELVsPy2i7pHG7R5VWQUSorEKZ17QVgkH";
}