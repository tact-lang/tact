;;
;; Header files for Empty
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_verify_address
slice __tact_verify_address(slice address) inline;

;; $Empty$_contract_init
tuple $Empty$_contract_init() impure inline_ref;

;; $Empty$_contract_load
tuple $Empty$_contract_load() impure inline_ref;

;; $Empty$_contract_store
() $Empty$_contract_store(tuple v) impure inline;
