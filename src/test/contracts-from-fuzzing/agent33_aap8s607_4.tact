contract TestBinaryOps {
    receive() {
        let add: Int = 1 + 2;      // addition
        let sub: Int = 5 - 3;      // subtraction
        let mul: Int = 3 * 4;      // multiplication
        let div: Int = 10 / 2;     // division
        let mod: Int = 10 % 3;     // modulo operation
    }
}
