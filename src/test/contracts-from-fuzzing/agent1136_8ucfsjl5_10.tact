contract MixedParamsOpt(
    owner: Address,
    count: Int,
    active: Bool,
    description: String?,
    note: String,
    optNumber: Int?,
    emptyMaybe: Cell?,
    bigFlag: Bool?,
    sliceThing: Slice?,
    initialized: Bool,
) {
    // contract with many mixed parameters
}