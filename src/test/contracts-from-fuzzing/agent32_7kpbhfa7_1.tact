contract Test {
    // Test 1: Basic empty message receiver
    
    // This receiver handles empty messages, matching the exact signature "receive()"
    receive() {
        // Code body is empty
    }

    // Fallback declarations are not included in this test
}
