contract TestContract {
    receive() {
        // Testing binary operators
        let a: Int = 10;
        let b: Int = 20;
        // Addition
        let sum: Int = a + b;
        // Subtraction
        let diff: Int = a - b;
        // Multiplication
        let prod: Int = a * b;
        // Division: using integer division
        let quo: Int = a / 2;
        
        return;
    }
}
