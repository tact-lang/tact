contract LongParams(
    a: Int,
    b: Bool,
    c: String,
    d: Int?,
    e: Bool?,
    f: String?,
    g: Address,
    h: Address?,
    i: Cell,
    j: Cell?,
    k: Int,
    l: Bool
) {
    // intentionally no init, parameters act as storage
}