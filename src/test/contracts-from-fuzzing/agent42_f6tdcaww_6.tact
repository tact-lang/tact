contract BounceTestRaw {
    bounced(rawMsg: Slice) {
        // Processing raw bounced message
        // Here we might attempt to parse the raw message if needed
        // For now, just a no-op
    }
}
