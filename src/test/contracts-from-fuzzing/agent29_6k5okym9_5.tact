contract StringTest {
    fun testLongString(): String {
        return "This is a very long string that serves as a test for the Tact compiler to handle string literals properly without any issues or truncation. It should ideally work just fine and not produce any errors.";
    }
}
