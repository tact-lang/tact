contract Test {
    // Test for empty message receiver
    receive() {
        // Minimal implementation
    }

    // Dummy function to ensure contract is used
    get fun dummy(): Int {
        return 0;
    }
}
