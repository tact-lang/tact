contract TestBounceRaw {
    bounced(rawMsg: Slice) {
        // Bounce receiver using raw Slice parameter
        // No processing logic; valid as per documentation for handling all bounced messages
    }
}
