contract Test {
    // Test 1: Empty message receiver
    receive() {
        // Minimal implementation for empty messages
    }

    // Added a dummy function to force compilation of the contract
    fun dummy() {}
}