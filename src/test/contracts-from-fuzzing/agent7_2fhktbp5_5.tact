contract TestBinaryArithmetic {
    receive() {
        // Binary Addition
        let add: Int = 10 + 20; // 30
        // Binary Subtraction
        let sub: Int = 50 - 10; // 40
        // Binary Multiplication
        let mul: Int = 4 * 5;   // 20
        // Binary Division
        let div: Int = 50 / 2;  // 25
    }
}
