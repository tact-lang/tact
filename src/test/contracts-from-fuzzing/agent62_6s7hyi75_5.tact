contract TestUnaryOperators {
    receive() {
        // Testing unary operators: negation, logical not, bitwise not
        let a: Int = -42;
        let b: Bool = !true;
        let c: Int = ~0;
    }
}
