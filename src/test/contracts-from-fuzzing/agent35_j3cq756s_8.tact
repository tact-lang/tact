contract BinaryOperators {
    receive() {
        let add = 1 + 2;
        let sub = 5 - 3;
        let mul = 3 * 4;
        let div = 8 / 2;
        let logicAnd = true && false;
        let logicOr = false || true;
        let ternary = (1 + 2 == 3) ? 100 : 200;
    }
}
