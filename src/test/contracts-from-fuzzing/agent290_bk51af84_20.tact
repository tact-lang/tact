contract Test(
    a: Int,
    b: Bool,
    c: String,
    d: Address,
    e: Int?,
    f: Bool?,
    g: String?,
    h: Address?,
    i: Int as uint64,
    j: Int? as uint8,
    k: Bool,
    l: String
) {
    // no init(), rely on contract params only
}