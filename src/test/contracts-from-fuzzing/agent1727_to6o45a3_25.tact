// contract using contract parameter persistent fields only primitives
contract ParamsPrimitives(
    a: Int,
    b: Int,
    c: Bool,
    d: Bool,
    s: String,
    t: String,
) {
    receive() {
        /* noop */
    }
}