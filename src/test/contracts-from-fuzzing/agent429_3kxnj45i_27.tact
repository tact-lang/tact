contract LargeContractParams(
    a: Int,
    b: Bool,
    c: String,
    d: Slice,
    e: Cell,
    f: Address,
    g: Int?,
    h: Bool?,
    i: String?,
    j: Slice?,
    k: Cell?,
    l: Address?,
) {
    // No init() allowed with contract parameters
}
