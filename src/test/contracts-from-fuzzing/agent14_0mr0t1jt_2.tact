asm fun swap(a: Int, b: Int): Int { SWAP }

fun main() {
    // Testing swap: expected output is 10
    let result = swap(10, 20);
    // We return a string to see the result in logs (not strictly supported, but assuming debugging)
    result;
}
