contract Test {
    // Test 3: Arbitrary text message receiver function
    // It should match any received text that doesn't match specific comment receivers
    receive(str: String) {
        // No operation
    }
}
