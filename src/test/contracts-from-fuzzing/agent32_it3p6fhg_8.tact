fun dynamicAddress(s: String): Address {
    // This function attempts to create an Address from a dynamic string.
    // The built-in address() function expects a valid address string literal.
    return address(s);
}
