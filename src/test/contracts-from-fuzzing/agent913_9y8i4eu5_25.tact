// Long list of primitive and optional fields in contract storage via contract parameters

contract BigParams(
    a: Int,
    b: Int?,
    c: Bool,
    d: Bool?,
    e: String,
    f: String?,
    g: Cell,
    h: Cell?,
    i: Slice,
    j: Slice?,
    k: Builder,
    l: Builder?,
    m: Address,
    n: Address?
) {
    // Just the declaration
}