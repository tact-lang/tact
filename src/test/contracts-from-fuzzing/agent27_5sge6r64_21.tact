// per docs, can't combine param list with fields; test with only param list but many params
contract TestContract(
    a: Int,
    b: Bool,
    c: String,
    d: Int?,
    e: Bool?,
    f: String,
    g: Address,
    h: Cell,
    i: Int,
    j: Bool
) {
}
