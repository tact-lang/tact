contract TestBinaryOps {
    receive() {
        // Addition operator
        let sum: Int = 1 + 2;
        
        // Subtraction operator
        let diff: Int = 5 - 3;
        
        // Multiplication operator
        let prod: Int = 2 * 3;
        
        // Division operator
        let quot: Int = 10 / 2;
    }
}
