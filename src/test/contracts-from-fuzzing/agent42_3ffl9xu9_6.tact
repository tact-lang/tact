contract TestBinaryArithmetic {
    receive() {
        let add: Int = 1 + 2;
        let sub: Int = 5 - 3;
        let mul: Int = 2 * 3;
        let div: Int = 8 / 2;
        let mod: Int = 10 % 3;
    }
}
