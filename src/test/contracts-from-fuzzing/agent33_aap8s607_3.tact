contract TestLiterals {
    receive() {
        let a: Int = 42;          // positive integer literal
        let b: Int = -7;          // negative integer literal
        let c: Bool = true;       // boolean literal true
        let d: Bool = false;      // boolean literal false
        let e: String = "hello";  // string literal
    }
}
