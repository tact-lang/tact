contract Test {
    // This receiver handles arbitrary text messages
    receive(str: String) {
        // Simply echo the message by some means
        // (in a real example, additional operations would be here)
    }
}
