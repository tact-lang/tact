contract Test {
    // Test case 1: Simple empty message receiver
    receive() {
        // No operation
    }

    // To ensure the function is used, we include a dummy function call via a message passing simulation placeholder.
    // In actual Tact, sending a message would trigger this function.
}
