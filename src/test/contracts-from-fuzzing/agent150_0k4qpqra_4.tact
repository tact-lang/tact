contract Test2 {
    // Test for receive with a String parameter (text receiver for arbitrary text messages)
    receive(str: String) {
        // empty implementation to ensure the function compiles
    }
}
